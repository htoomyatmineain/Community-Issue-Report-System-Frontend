import { useState } from "react";

/** Feature-local hook for admin-staff. Replace with real data fetching/state. */
export function useAdminStaff() {
  const [state, setState] = useState(null);
  return { state, setState };
}
